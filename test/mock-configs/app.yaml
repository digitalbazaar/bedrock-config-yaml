test-bedrock-module:
  foo: fromYaml
  overwriteMe: fromYaml
test-transformer-app:
  echoed: {$TEST-ECHO: hello}
  fromEnv: {$ENV: TEST_CONFIG_YAML_APP}
  port: {$ENV: {name: TEST_CONFIG_YAML_PORT, type: number}}
  defaulted: {$ENV: {name: TEST_CONFIG_YAML_UNSET, default: fallback}}
